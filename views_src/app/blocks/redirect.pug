if !email
    script.
        localforage.getItem("me")
                    .then(function(me) {
                        if (me) {
                            $('#spinnerdiv').fadeOut();
                        }
                        else { 
                            window.location.replace('/');
                        }
                    }).catch(function(err) {
                        console.log(err);
                        window.location.replace('/');
                    });
else
    script.
        setTimeout(function() {$('#spinnerdiv').fadeOut();}, 800);
