if !email
    script.
        localforage.getItem("me")
                    .then(function(me) {
                        if (me) {
                            $('#spinnerdiv').fadeOut();
                        }
                        else { 
                            console.log('me do not exist');
                            window.location.replace('/');
                        }
                    }).catch(function(err) {
                        console.log('hey!')
                        window.location.replace('/');
                    });
else
    script.
        setTimeout(function() {$('#spinnerdiv').fadeOut();}, 800);
